package capitalone;

/**
 * Reconstructed with Claude.
 *
 * Lamp Radius
 *
 * Given:
 * - start — the lamp's initial x position on a horizontal line
 * - steps — number of steps the lamp takes
 * - stepSize — distance the lamp moves per step (always moving right)
 * - radius — the light radius of the lamp
 *
 * The lamp starts at start and moves right steps times, advancing stepSize each step. After all steps are complete,
 * return the two x-coordinates of the edges of the lit area.
 *
 * Example:
 *
 * start=0, steps=3, stepSize=2, radius=5
 *
 * final position = 0 + 3*2 = 6
 * left edge = 6 - 5 = 1
 * right edge = 6 + 5 = 11
 *
 * output: [1, 11]
 */
public class Exercise4 {

    public static int[] solution(int start, int steps, int stepSize, int radius) {
        // TODO
        return new int[0];
    }
}
